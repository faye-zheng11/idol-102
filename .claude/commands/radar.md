---
description: 雷达——AI 主动去抓：GitHub 可用工具 / 产品思维·用户情报 / AI 模型变动
argument-hint: "[频道: github | insight | 模型 | 留空=全扫]"
---

按 `workbench/playbooks/radar.md` 开雷达：

1. 看扫哪个频道（$ARGUMENTS；留空=三个都扫）。
2. 用 WebSearch/WebFetch 去抓：🛠 GitHub 适合 PM 的项目/skill · 💡 最新产品思维/用户研究 · 🤖 各家 AI 模型变动（Claude 侧用 `claude-api` 拿准数据）。
3. 提炼要点 + 标日期+出处，落简报到 `workbench/radar/YYYY-MM-DD-<频道>.md`。
4. 推荐要不要装/晋级：markdown skill 可直接装进 `.claude/skills/`（记 NOTICE 来源）；含代码/脚本的**先给用户过目**再装；成体系方法论晋级 `references/`。
5. 模型频道结合本项目用途（角色对话/情感陪伴/Scene/翻译语音）给选型方向。

想定时自动扫：用 `schedule` 技能挂个每周任务。
